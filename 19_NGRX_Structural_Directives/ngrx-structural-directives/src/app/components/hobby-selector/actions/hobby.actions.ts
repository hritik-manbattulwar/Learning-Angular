import { createAction, props } from '@ngrx/store';

export const checkEating = createAction('[Hobby] Eating');
export const checkCoding = createAction('[Hobby] Coding');
export const checkSleeping = createAction('[Hobby] Sleeping');
